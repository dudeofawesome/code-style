{
  "name": "@code-style/utils",
  "version": "2.0.0-27",
  "private": true,
  "scripts": {
    "test": "shopt -s globstar; node $NODE_OPTS --require tsm --test **/*.test.ts",
    "test:debug": "NODE_OPTS='--inspect-brk' npm run test"
  },
  "dependencies": {
    "eslint": "^9.0.0",
    "stylelint": "^16.3.1"
  },
  "engines": {
    "node": ">=18"
  }
}
