rules:
  # use `else if` instead
  no-lonely-if: 'error'

  # ensure any constant numbers are properly named to increase readability
  no-magic-numbers:
    - 'error'
    - enforceConst: true
      # TODO(0): consider adding 4, 24, 60, 100, 1000, 3600
      ignore: [0, 1, 2]
      ignoreArrayIndexes: true
      ignoreDefaultValues: true
      ignoreClassFieldInitialValues: true

  # don't allow floating decimals
  no-floating-decimal: 'error'

  # require dot notation when possible
  dot-notation: 'error'

  # TODO: what should our max be? should we even have one?
  max-lines: ['error', { max: 2000, skipBlankLines: true, skipComments: true }]

  # treat method params as const
  no-param-reassign: 'warn'
